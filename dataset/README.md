# Dataset preparation
Please download dataset files from Amazon: https://cseweb.ucsd.edu/~jmcauley/datasets/amazon/links.html
These files should be included:
1. Rating file in `Files/Small subsets for experimentation`  
2. Meta files in `Per-category files`, [metadata], [image features], [reviews]  
   
Each dataset file is placed in a separate folder